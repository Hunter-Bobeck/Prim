using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AlgorithmController : MonoBehaviour
{
    public Controller controllerLeft, controllerRight;      // connections for both controller scripts

    void Update()
    {
        /*if (controllerLeft.menu_button_pressing() || controllerRight.menu_button_pressing())        // when either of the controller's menu buttons is pressing
        {
            
        }*/
    }
}
